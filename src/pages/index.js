import Head from "next/head";
import Band from "@/components/Band";

export default function Home({ data }) {
  //console.log(data);
  return (
    <>
      <Head>
        <title>Foofest</title>
      </Head>
    </>
  );
}

